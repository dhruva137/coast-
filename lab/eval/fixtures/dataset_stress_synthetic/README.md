# dataset_stress_synthetic (SYNTHETIC)

Tiny fixture for `python lab/stress/run_dataset_stress.py lab/eval/fixtures/dataset_stress_synthetic --adapter synthetic`.

**Not field proof.** Drop real downloads under `data/field/` and score locally — do not fork into a Kaggle notebook.
