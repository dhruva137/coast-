# field_session_dry_run (SYNTHETIC)

RecordService-shaped fixture for python -m lab.eval.score_field_log --dry-run.

**Not a real field log.** Real scooter/bicycle loop-closure logs are still
pending (see lab/stress/results/CURRENT_VERDICT.md). Do not cite dry-run
numbers as field proof.
