# Judge cross-examination pack — SIH 26168

**Premise:** You have a serious evaluation lab, not a beaten ISRO bar (unless measured rows say otherwise). Canonical truth: `lab/stress/results/CURRENT_VERDICT.md`.

**Destroy credibility instantly if you:** sell prototype PASS as ISRO; call injected lean “scooter field”; hide 93% / 0 ISRO tables; claim AVNet window RMSE as closed-loop ATE; claim OSM map when geometry is in-dataset polyline.

---

## Top hostile questions (answer strategy)

### 1. Best real 60 s denied result?
Open alignment / hardened tables. Say the number first. Today: open-loop **fails**; map-aided at best **competitive low-speed** unless `avnet_closed_loop` summary shows new PASS rows.

### 2. Naive ~93% — fatal?
That is the **baseline failure** we advertise. Credibility = diagnosis (heading) + closed-loop stack.

### 3. Meet ISRO &lt;10% / &lt;100 m/km?
**Only if** `lab/stress/results/avnet_closed_loop/summary.md` (or CURRENT_VERDICT) shows measured `PASS_ISRO`. Else: **No.**

### 4. Wrong yaw axis?
`ALIGNMENT_REPORT.md`: `gz=-Pitch`; S-S1/S-S2 strong correlation. Admit weaker files.

### 5. AVNet 1.04 m/s RMSE = ATE?
**No.** Window supervision only. Closed-loop = `avnet_speed` / `avnet_full` rows.

### 6. Reproduce Qian tunnel metres?
Different rate (200 Hz vs 10 Hz). We claim 10 Hz–correct tiny prior, not their tunnel metres.

### 7. Oracle speed still fails?
Heading/bias dominates (`WORLD_REALITY_REPORT.md`).

### 8. Map 15 m at 1.85 m/s?
Known-route, low-speed, one scenario — not road-speed ISRO.

### 9. Injected lean 3.84%?
`INJECTED_LEAN` counterfactual only. TW field **RED**.

### 10. Coordinated-turn failure?
Hard brake mid-turn (`DISCLOSURE.md`).

### 11. Is lean novel?
Kinematics textbook; claim = phone fixed-point + branch metric + field protocol.

### 12. Branch vs drift?
F12: wrong ramp can look “few %” Euclidean. Operational metric.

### 13. Covariance honest?
Blind coverage ~25% — we **fail** our deployment gate.

### 14. Blind challenge cherry-picked?
`PROOF_PROTOCOL.md` commit–reveal; proves protocol, not deployment accuracy.

### 15. 10 Hz latency?
`metrics.json` infer ~3.2 ms. Distinguish logger vs full product filter.

### 16. Scooter ride?
Android RECORD ready; **0 qualifying TW logs** until collected.

### 17. Live vs precomputed?
Prefer live Evidence Room with FAIL numbers visible.

### 18. 90-day ship plan?
Sensor contract → closed-loop AVNet → OSM graph → ≥10 TW loops → calibrated cov.

### 19. Any mount?
Require clamp + alignment gate.

### 20. Why cars for TW PS?
IO-VNBD is official public set; lean novelty needs TW later.

### 21. Mag/light?
Mag likely unusable; light under-tuned sim only.

### 22. Train/eval leakage?
State splits; volunteer known-route map caveat.

### 23. Prototype PASS marketing?
Read aloud: `RESEARCH_PROTOTYPE_PASS` ≠ deployment.

### 24. Prior art?
Own narrow claims; cite Qian 2025, AI-IMU, DeepOdo, Newson–Krumm.

### 25. Fund Monday — one experiment?
(1) Closed-loop AVNet table (2) scooter KEEP logs (3) OSM branch PF.

---

## 5-minute demo script

0:00 Problem + bar — **we do not claim ISRO pass unless table says so**  
0:40 Failure first — open-loop RED / ~93%  
1:20 Axis audit GREEN  
2:00 Injected lean A/B (labelled)  
2:40 AVNet prior + closed-loop table  
3:20 Map / branch metric honesty  
4:00 Android RECORD protocol  
4:35 “Ask anything against CURRENT_VERDICT”

Full architecture: `docs/FINALS_WINNER_ARCHITECTURE.md`
